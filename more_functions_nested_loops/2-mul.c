#include "main.h"
/**
* mul - multiplies two integers
* @a: the first number integer to multiply
* @b: the second number integer to multiply
* Return: The result of multiplication between x param and y param
*/
int mul(int a, int b)
{
	return (a * b);
}
