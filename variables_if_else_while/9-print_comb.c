#include <stdio.h>
/**
 * main - single-digit numbers
 *
 * Return:0
 */
int main(void)
{
	int n, i;

	for (n = 0; n <= 9; n++)
	{

	for (i = 0; i <= 9; i++)
	{
		printf("%d%d ", n, i);
}
}

printf("\n");
return (0);
}
