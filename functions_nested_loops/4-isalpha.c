#include "main.h"
/**
 * _isalpha - Checks for alphabetic character
 *
 * Return:1 for lowercase and uppercase or 0 for anything else
 */
int _isalpha(int c)
{
	if (c >= 97 && c <= 122)

	if (c >= 66 && c <= 90)
		{
			return (1);
		}
		return (0);
}

